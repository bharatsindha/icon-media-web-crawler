# Web Crawler Dependencies

# HTTP requests
requests==2.31.0
urllib3==2.1.0

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.1.0

# Compression support for Brotli-encoded responses
brotli==1.1.0

# Database (choose one based on your Python version)
# For Python 3.12+ (recommended for newer Python versions)
psycopg[binary]==3.2.3
psycopg-pool==3.2.5

# For Python < 3.12 (uncomment if psycopg3 fails)
# psycopg2-binary==2.9.9

# Environment configuration
python-dotenv==1.0.1

# YAML configuration
PyYAML==6.0.1

# Optional: Better performance
# httpx==0.27.0  # Alternative to requests with HTTP/2 support
